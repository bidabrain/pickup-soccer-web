# 本地密钥模板。复制为 .dev.vars 后填入真实值（.dev.vars 已被 .gitignore 排除）。
# 线上用 `wrangler secret put NAME` 设置，绝不提交到 repo。

# 可选：全局额外盐，增强 HMAC 判重抗拖库能力（M2 启用）
# HMAC_PEPPER=replace-with-a-long-random-string

# Android 推送通知（Firebase Cloud Messaging HTTP v1）
# 获取方式：Firebase 控制台 → 项目设置 → 服务账号 → 生成新的私钥 → 下载 JSON
# 将整个 JSON 文件内容（单行）存为 secret：
# wrangler secret put FIREBASE_SERVICE_ACCOUNT
# 不填则静默跳过推送，不影响其他功能
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...","private_key":"...",...}
