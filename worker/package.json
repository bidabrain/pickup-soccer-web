{
  "name": "pickup-soccer-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:schema:local": "wrangler d1 execute pickup-soccer --local --file=schema.sql",
    "db:schema:remote": "wrangler d1 execute pickup-soccer --remote --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
