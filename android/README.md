# Pickup Football · Android 客户端

本目录用于开发 Pickup Football 网站的安卓客户端。

后端 API 与网站共用：`https://pickup-soccer-api.bidabrain.workers.dev`

> 注意：本目录的改动**不会**触发网站（GitHub Pages / Worker）的重新部署——
> 部署只在 `web/` 或 `worker/` 改动时触发（见 `.github/workflows/deploy.yml`）。
